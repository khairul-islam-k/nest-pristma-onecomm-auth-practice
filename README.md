## Project setup

```bash
npm install @nestjs/passport passport
npm install @nestjs/jwt passport-jwt
npm install bcrypt
npm install -D @types/passport-jwt @types/bcrypt

npm install passport-local @nestjs/passport
npm install -D @types/passport-local
```
